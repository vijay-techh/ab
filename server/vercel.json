{
  "version": 2,
  "builds": [
    { "src": "api/server.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "/(.*)$",
      "dest": "/server.js"
    },
    {
      "src": "^/(.*)$",
      "dest": "/client/$1"
    }
  ]
}
